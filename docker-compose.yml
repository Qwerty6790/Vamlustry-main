services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: вамлюстра-nextjs
    restart: always
    environment:
      - NODE_ENV=production
      - PORT=3007
      - NEXT_PUBLIC_API_URL=https://elektromos-backend.vercel.app/api
    networks:
      - app-network

  nginx:
    image: nginx:alpine
    container_name: вамлюстра-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./certbot/www:/var/www/certbot:rw
      - ./certbot/conf:/etc/letsencrypt:rw
    depends_on:
      - nextjs
    networks:
      - app-network

  certbot:
    image: certbot/certbot
    container_name: вамлюстра-certbot
    depends_on:
      - nginx
    volumes:
      - ./certbot/www:/var/www/certbot:rw
      - ./certbot/conf:/etc/letsencrypt:rw
    command: >
      certonly --webroot
      --webroot-path=/var/www/certbot
      --email admin@вамлюстра.ru
      -d вамлюстра.рф
      -d www.вамлюстра.рф
      --agree-tos
      --no-eff-email
      --force-renewal

networks:
  app-network:
    driver: bridge
